# DecisionAnalysis — Spec

Not written yet. This scaffolding pass only set up the project and
deploy pipeline — no feature spec has been finalized.

To be filled in based on the functionality checklist discussed
separately (tree building, EV/VOC calculation, flip/split view,
save/load, undo/redo, PNG export, etc.) before any of it is built.
