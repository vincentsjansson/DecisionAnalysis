# DecisionAnalysis — Målspec (TypeScript, from scratch)

Status: ✅ byggt · ⚠️ delvis/risk · ❌ ska byggas.

Beslut 2026-07-25: C# WPF och Python-backend skrotas. Allt byggs i TypeScript som en statisk webbapp (GitHub Pages), inget backend krävs — beräkningarna är enkla nog att köra client-side.

**Teknikstack:** Vite + vanilla TypeScript, SVG-rendering av trädet, deploy till GitHub Pages via GitHub Actions. Krav: en extern, icke-teknisk person ska kunna klicka en länk och få upp verktyget direkt i webbläsaren, inget installationssteg.

## MVP-prioritering (bekräftad 2026-07-27)

Kursen (Grunderna för beslutsfattande och beslutsanalys, vecka 2–10) har ett mycket större kravomfång än vad som byggs nu. MVP:n är medvetet avgränsad till fyra kärnfunktioner: **trädvisualisering, EV, EU, VOC**. Allt annat (VOI för imperfekt information, känslighetsanalys, flerdimensionella värdefunktioner, relevansdiagram, risk & förmåga-modul, beslutsanalyscykel-vy) är explicit utanför MVP och kommer som senare, separata segment.

**Byggordning för MVP:**

1. ✅ Modellrefaktor (två nodtyper `decision`/`chance`, payoff på terminala outcomes, villkorstabeller per nod) — klar.
2. ✅ VOC (flip/split med Bayes-omvändning för klassisk klarsyn) — klar.
3. ✅ EU (Expected Utility) — u-funktion i två former (linjär + exponentiell CARA med parameter γ, reviderat 2026-07-27 efter kursmaterialet), EV räknas om på nyttovärden och transformeras tillbaka till säkerhetsekvivalent (CE) för visning. γ sätts via elicitering (indifferens-fråga eller referensbelopp), inte råinmatning. Klar.
4. ✅ Pedagogiska tillägg: sannolikhetsvalidering som synlig UI-varning (Σ-indikator på noden, resolved-prob-medveten) och steg-för-steg-beräkningsvisning (trace-bar för vald nod + terminal-nyttotransform i EU-läge). Klar. **MVP komplett.**

**Explicit uteslutet ur MVP (senare, egna segment):** VOI för imperfekt information (kräver Bayesiansk uppdatering, likelihood, sensitivitet/specificitet — bygger vidare på VOC-ramverket), känslighetsanalys, flerdimensionella värdefunktioner, relevansdiagram, risk & förmåga-modul, beslutsanalyscykel-vy, real optioner, PNG-export, save/load-UI.

## Bekräftad regel att inte återupprepa

Tidigare C#/Python-kod hade en fältnamnsregression: C# serialiserade `conditionaltables`, Python läste `conditional_tables`. **TS-koden ska konsekvent använda `conditional_tables` (snake_case) i alla JSON-strukturer.**

## Lärdomar från legacy-genomgången (2026-07-27)

Den lokala kopian av det gamla projektet (inte GitHub-versionen, som var betydligt tunnare — se PROGRESS.md) visade sig ha en nästan komplett WPF-app: canvas-rendering, sequence bar, dialoger, save/load, och en fungerande FastAPI-backend med riktig EV- och backward-fill-logik. En fullständig genomläsning av `legacy/frontend/csharp-wpf/MainWindow.xaml.cs` (1171 rader), `ViewModels/TreeViewModel.cs`, och `legacy/backend/treelogic.py` gav tre arkitekturbeslut som nu är låsta för TS-bygget, plus en lista referensbuggar.

### Låsta beslut

1. **Äkta träd, inte sequence-UI.** Legacy's `TreeViewModel.Sequence` är en **platt lista av nivåer** — `RebuildLinks()` låter alla outcomes på nivå i peka på **samma** nod-objekt på nivå i+1. Det är därför asymmetriska träd är omöjliga i legacy, och därför `LeafValues`/`NodeEvValues` måste vara path-keyed (noderna delas mellan alla paths). TS-datamodellen (segment 3) är redan ett äkta träd. Rendering/UI-segmentet ska bygga interaktion runt riktiga, potentiellt asymmetriska träd (klicka en nod → lägg till barn på just den grenen) — **inte** en pill-sequence-bar.

2. **Flip/split: riktig Bayes-omvändning, inte legacy's naiva kopiering.** Legacy kopierar sannolikheterna oförändrade till det speglade trädet. Det är bara matematiskt korrekt om chansnodernas sannolikheter är oberoende av tidigare grenar — med villkorade sannolikheter i scope är naiv kopiering fel och skulle ge tyst felaktig VOC, oacceptabelt i ett läromedel. Flip/split blir därför ett eget, större segment (kräver korrekt beräkning av marginaler och posteriorer via Bayes), inte en enkel spegel-operation. **Flip/split byggs inte som en del av det kommande rendering/UI-segmentet — det skjuts upp till ett dedikerat framtida segment.**

3. **Backward-fill: behåll "justera första justerbara nod längs pathen"-regeln**, samma som legacy, men gör den transparent — rapportera tydligt till användaren vilken kant/sannolikhet som faktiskt ändrades, istället för att bara logga det tyst (legacy loggade bara till `Debug.WriteLine`).

### Referensbuggar att inte upprepa

Dessa är redan förhindrade av TS-modellens grundarkitektur (ett villkorsformat, fel istället för tyst normalisering, äkta träd, id:n istället för namn), men värda att ha i huvudet vid UI-bygget:

- **Tre olika nyckelformat för villkorade sannolikheter** i legacy samtidigt: UI:t skapade `"Nod = Outcome"`, `forward_probability_along_path` använde bara outcome-namnet, `traverse_tree` använde `"Nod:Outcome"`. Ingen av dem matchade varandra — subset-matchningen kunde aldrig träffa något UI:t skickat, och `calculate_ev` anropade aldrig villkors-upplösningen alls. Skärmen visade villkorade sannolikheter (UI:ts egen lokala beräkning); root-EV räknades på bassannolikheterna. TS: ett enda format (`nodId:label`), och EV-beräkningen använder samma history-mekanism som resolvern.
- **VOC-knappen var en återvändsgränd** — Run EV fanns bara på vänster träd, så högerträdets EV (som VOC kräver) kunde aldrig sättas.
- **Remove-node skyddade åt fel håll** — blockerade borttagning av en nod med egna conditional tables, men tillät borttagning av en nod som *andra* noders tabeller refererade till (blev tysta döda villkor).
- **Rename bröt namnbaserade conditional-table-referenser tyst** — villkor och tabellrader var nycklade på outcome-namn, så en omdöpning lämnade skräpnycklar utan varning.
- **Tre olika toleranser för sannolikhetssumma** i olika lager (1e-5 i UI-varningen, 0.01 i spara-spärren, 1e-6 i backend).
- **Kantetiketter placerades blint** på Bézier-mittpunkten med fasta pixel-offsets, ingen kollisionshantering — bekräftar "label overlap"-buggen från den ursprungliga checklistan.

## Nodtyper (reviderat beslut 2026-07-27)

- ✅ **Två** nodtyper: `decision` och `chance`, med explicit `nodeType`-fält (`src/model/tree.ts`). **Utfallen är kanterna** från noden (`Outcome`), och ett terminalt utfall (utan barn) är slutpunkten och bär payoff-värdet (`Outcome.value`) — det finns ingen separat lövnodtyp. Detta matchar läroboksmodellen och legacy's semantik, och löser både namnkollisionen (`Outcome`-klass vs `'outcome'`-nodtyp) och etikettdupliceringen som den tidigare tre-typsmodellen hade.

## Datamodell

- ✅ `TreeNode` / `Outcome` i TS (`src/model/tree.ts`): `addOutcome` (unika syskon-etiketter), `setChild`/`detachChild`/`removeOutcome`/`renameOutcome`. Osatt sannolikhet = `NaN`, osatt payoff = `undefined` — visas som "–", aldrig fabricerade nollor.
- ✅ Villkorstabeller lagras **per nod** (reviderat 2026-07-27, som legacy): varje rad = villkor (set av `nodId:utfall`-tokens) → komplett fördelning över nodens utfall. Radsumma-1 är därmed strukturellt naturlig, till skillnad från per-kant-overrides. Subset-matchning mot history, mest specifika rad vinner, lika specifika kastar `AmbiguousConditionalProbabilityError` (`src/model/conditionalProbability.ts`). Utfall som en matchande rad inte täcker faller tillbaka på bassannolikheten. **UX (2026-08-02, segment 20):** villkorspickern erbjuder bara förfäder-utfall på nodens *faktiska väg* (`availableTokens` = path tokens) — i ett äkta träd har varje instans exakt en väg, så ett villkor på en syskongren kan aldrig matcha; döda rader kan inte längre skapas. Dialogen visar även en "Gäller för den här instansen"-rad med den **upplösta** fördelningen (`resolveProbability`) så bas vs matchande villkorsrad aldrig är tvetydigt.
- ✅ Cykel-skydd vid nod-koppling: `setChild` går upp genom `parent`-kedjan och kastar `CyclicTreeError` om barnet redan är en förfader (inkl. self-loop).
- ✅ `renameOutcome` skriver om både nodens egna villkorsraders nycklar och alla `nodId:etikett`-tokens i hela trädet — rename kan inte tyst döda villkor (legacy-buggen).
- ✅ **Länkade variabelinstanser** (`src/model/variable.ts`, reviderat 2026-07-27/28): samma konceptuella variabel kan förekomma som separata nodinstanser på olika grenar. `TreeNode` har `variableId` (grupp; singleton = eget id) och `instanceIndex` (0 = primär, driver prim-markörer via `displayName` — den primade strängen lagras aldrig i `label`). Skapa en nod med ett befintligt namn → auto-länkas (delad `variableId`, kopierad utfalls*uppsättning*, typkonflikt kastar `VariableConflictError`). **Rekursiv/tvärgående mirroring (`mirrorLinkedInstances`, 2026-07-28):** när en nod skapas under en **slumpnods** utfall speglas den över hela rutnätet `grupp(förälder) × utfall` — dvs. både (a) förälderns *egna* övriga terminala utfall OCH (b) motsvarande utfall (samma label) på förälderns länkade syskoninstanser P', P''. Alla skapade noder bildar en egen länkad grupp. Screenshot-fallet: nämen/nämen'/nämen'' (3 instanser, utfall 1/2/3) + "okej" under nämen:1 → 9 okej-instanser över hela 3×3-rutnätet, en delad `variableId`. En **enda bunden pass** per skapande-event (|grupp|×|utfall|, aldrig en trädtäckande sökning); djup komponeras naturligt eftersom varje efterföljande nodskapande triggar samma handler för sin egen grupp (en nyskapad nod är barnlös, så ingen rekursion behövs i passet). Endast terminala positioner fylls (befintlig/frikopplad/avvikande struktur lämnas orörd — no-overwrite på valfritt djup); gäller **inte** beslutsnoder (asymmetriska beslut bevaras — klassiska VOC-exemplet). Positionsmappningen mellan instansers utfall är label-baserad (samma nyckel som utfalls-synken). Utfallsuppsättningen och **nodtypen** hålls **alltid** synkad över gruppen (add/remove/rename samt typbyte propagerar; villkorssäker rename skriver om varje instans egna villkorsnycklar + tokens). **Sannolikhetssynk (DESIGNÄNDRING 2026-08-02, ersätter tidigare "sannolikheter är egna per instans"):** platta utfallssannolikheter synkas nu **som standard** över gruppens instanser (`syncProbabilitiesFromNode`) — motivet är snabbare trädbyggande: fyll i en gång på valfri instans, alla andra antar samma fördelning (alla instanser skapas först via mirroring, så en enda ifyllning når hela gruppen). **Undantag:** en instans med egen `conditional_table` blir kontextberoende och **slutar automatiskt synka** sina sannolikheter (varken skickar eller tar emot) utan att man behöver "Koppla loss" — den behåller dock koppling för utfallsuppsättning/nodtyp. Tas tabellen bort igen återgår instansen till synk och antar gruppens delade fördelning från en tabell-lös syskoninstans (`adoptGroupProbabilities`). Att döpa om en instans döper hela variabeln (propagerar); explicit "Koppla loss" (`unlinkNode`) är den mer drastiska mekanismen som bryter **allt** (utfallsuppsättning, nodtyp OCH sannolikheter) och gör instansen till en helt egen variabel — **granulärt på valfritt djup** (t.ex. en nästlad okej' kan frikopplas utan att påverka nämen-nivån eller okej/okej''-gruppen), eftersom varje nod bär sin egen `variableId` oberoende av förälderns; framtida mirroring hoppar över den frikopplade grenen via no-overwrite. `variableId`/`instanceIndex` round-trip-serialiseras. **"Fråga vid avvikelse" (2026-08-02, segment 20):** eftersom synk är standard misslyckas den naturliga gesten att ge länkade instanser *olika* sannolikheter tyst (synk skriver över). Sparar man därför en instans med värden som skiljer sig från gruppens *redan definierade* fördelning öppnas en dialog med två val — **Uppdatera hela gruppen** (synka) eller **Bara den här instansen (koppla loss)**; en första ifyllning (gruppens fördelning ännu NaN) räknas inte som avvikelse utan synkar tyst. Divergent edit + val = ett undo-steg. **Bugg-fix (segment 20):** `unlinkNode` av **primär**-instansen var en no-op (gruppens `variableId` = primärens nod-id, så `node.variableId = node.id` ändrade inget) — nu flyttas kvarvarande instanser till en ny ägar-id så primärens id frigörs.

## Beräkningar

- ✅ EV per nod: slumpnoder = viktat medelvärde över utfallen (terminalt utfall bidrar med sitt `value`, barn med sitt EV); beslutsnoder = max. Rekursiv, fungerar på varje nod i trädet (`src/model/expectedValue.ts`).
- ✅ Backward-fill: `src/model/backwardFill.ts` — målet är ett terminalt utfall; justerar första justerbara noden längs pathen (slumpnod, >1 utfall, ej styrd av matchande villkorsrad), löser ut sannolikheten mot mål-joint, omskalar syskon proportionellt. Returnerar full rapport (nod, utfall, gammal/ny, syskonjusteringar) som UI:t visar i meddelanderaden — aldrig tyst (låst beslut #3). Kastar `BackwardFillError` vid onåbart mål.
- ✅ VOC (Value of Clairvoyance): `src/model/bayesReversal.ts` — klassisk klarsyn (chansvariabler flyttas före besluten). **VOC-display omdefinierad 2026-08-03 till `|EV_vänster − EV_höger|`** (riktningsagnostisk; se Flip/split-avsnittet) — för en färsk flip identisk med det tidigare `EV(omvänt) − EV(original)`. Hård invariant `ensureVocInvariant` (VOC ≥ 0, negativt kastar fel, float-brus klampas) körs fortfarande vid *generering* som självkontroll. Scope: alla paths måste möta samma variabler i samma ordning — variabelidentitet avgörs av `variableId` (länkade instanser), inte av tillfälligt lika namn (reviderat 2026-07-27); tidig terminering hanteras med läroboks-dupliceringsregeln; strukturkonflikt, olika utfallsmängder, beslutberoende chansfördelningar, dubblerade variabler och Σ≠1 ger specifika `FlipError` istället för tyst fel svar. Osatta sannolikheter propagerar till VOC = "–".
- ✅ Beräkningssteg-visning (`src/model/calculationTrace.ts`): trace-bar visar aritmetiken bakom vald nods värde — slumpnod EV "0.3 × 8 + 0.7 × 2 = 3.8" (resolved conditional probs), slumpnod EU "EU = 0.3 × 5.507 + 0.7 × 1.813 = 2.921 → CE = 3.454", beslutsnod "max(Ja: 3.8, Nej: 3) = 3.8 (välj Ja)", terminal-nyttotransform "u(8) = 6.321" i EU-läge. Live, mode-medveten, ~4 signifikanta siffror, "Ofullständig data"-fallback.
- ✅ Sannolikhetsvalidering: `src/model/validateProbabilities.ts` kastar `ProbabilitySumError` (med nod-id och faktisk summa) om villkorade sannolikheter för en nod inte summerar till 1 inom tolerans 1e-6 — normaliserar inte tyst. UI:t visar varningen icke-blockerande på noden ("Σ = 0.6 ⚠"), och skiljer på *fel* summa och *ofullständig* data ("p ofullständig" när sannolikheter är osatta).
- ✅ **Repeterande decimaler (2026-08-01):** "Normalisera"-knappen använder `distributeSumToOne` som avrundar varje sannolikhet till 6 värdesiffror men låter **sista utfallet absorbera avrundningsresten**, så jämn fördelning av tredjedelar/sjättedelar etc. summerar till exakt 1 (⅓ → 0.333333 / 0.333333 / 0.333334). Utan detta summerar tre nakna 0.333333 till 0.999999 och faller precis utanför 1e-6-toleransen. Fortfarande **explicit** (bara vid knapptryck), aldrig tyst normalisering vid inmatning.

## Flip / split

- ✅ **Höger träd är redigerbart och oberoende (DESIGNÄNDRING 2026-08-03, segment 21, ersätter "skrivskyddat, alltid härlett"):** Flip genererar höger träd från vänster (Bayes-omvändning, som förr) men det blir sedan **lika redigerbart som vänster** (payoff, sannolikheter, lägg till/ta bort noder, allt) och **helt oberoende** — vänster-redigeringar re-flippar det **inte** längre. `state.rightRoot` är egen state; `renderRightPane` ritar den (den re-beräknas inte varje render). Höger tar emot samma redigeringshandlare som vänster; `rootOf(node)`/`touchTree(node)` dirigerar varje mutation till rätt träd och flaggar höger-avvikelse.
- ✅ **Vänster är alltid källan.** Flip/Sammanfoga behandlar alltid vänster som källa och regenererar höger därifrån — aldrig tvärtom. Att gå in i split (Flip) regenererar höger färskt (skriver över tidigare manuella höger-redigeringar); Sammanfoga lämnar split men **behåller** höger träd (persisteras). Att ladda ett dokument återställer båda träden exakt (regenererar inte).
- ✅ **VOC omdefinierad: `|EV_vänster − EV_höger|` (2026-08-03), riktningsagnostisk** — spelar ingen roll vilket träd som är "original" eller "omvänt". För en färsk flip (höger = ren Bayes-baslinje) är detta identiskt med klassisk klarsyns-VOC (höger ≥ vänster). Efter manuell höger-redigering är det en **fri jämförelse**; korrektheten mot användaren hålls ärlig av avvikelse-indikatorn (nedan). I EU-läge: `|CE_vänster − CE_höger|`. `ensureVocInvariant` (VOC ≥ 0) körs fortfarande vid *generering* som självkontroll, men displayen använder absolutbeloppet av de två live-trädens värden.
- ✅ **Avvikelse-indikator:** när höger träd redigerats manuellt sedan sin Bayes-generering (`state.rightEdited`) visas en diskret badge "✎ redigerad sedan Sammanfoga" i höger panels header och pane-caption byter till "Omvänt träd — fri jämförelse", så användaren förstår att VOC inte längre är en strikt formell klarsynsvärdering. Ingen blockerande dialog.
- ✅ **Dubbelträds-persistens (kritisk följd, löst — ingen tyst dataförlust):** `document.ts` sparar nu **båda** träden (`right_tree`), plus `right_edited` och `split`. Dokumentversion höjd till **2**; v1-filer laddas fortfarande (saknade fält → inget höger träd/enkel vy). `idCounter` höjs över högsta `n<siffra>`-id i **båda** träden. Round-trip-testat att en höger-redigering överlever Spara→Ladda.
- ✅ Trädoberoende verifierat: det flippade trädet byggs som ett helt nytt träd (`flip_N`-id:n, egna nodkopior per gren — inga villkorstabeller, inga delade referenser). Nya noder som skapas vid höger-redigering får `n<siffra>`-id (kolliderar inte med `flip_N`).
- ✅ **Nodform/typ läser alltid `node.nodeType` (verifierat 2026-08-03, segment 21):** granskning + regressionstest bekräftar att shape-rendering, layout, EV/EU och `bayesReversal` aldrig antar typ från position (t.ex. "root är beslut"). Ett träd med **chansnod som rot** hanteras korrekt oavsett var i sekvensen chansen ligger. Punkt 1 i uppdraget var alltså verifiering + regressionstest, ingen beräkningsfix behövdes.
- ✅ **ALGORITMÄNDRING: full sekvensvändning, inte "chans-alltid-först" (2026-08-03/04, segment 22, ersätter den klassiska klarsyns-omvändningen från segment 6):** användaren klargjorde att omvändningen ska vända **HELA** nodsekvensen rakt av, oavsett typ — `a→b→c` blir `c→b→a` även om `b` är en beslutsnod och `a`/`c` är chansnoder. Detta är **inte** längre "värdet av klarsyn" i strikt beslutsteoretisk mening (en beslutsnod kan hamna före information den ursprungligen berodde på) — det är en ren strukturell/positionell spegling. En flyttad nod behåller **sina egna utfall och de sannolikheter som redan är kopplade till dem, oförändrade** — ingen Bayesiansk omräkning. `canonical`-sekvensen (en variabel per djup, validerad över alla vägar, tidig terminering via dupliceringsregeln) är **oförändrad** från segment 6 — bara `order = canonical.slice().reverse()` istället för chans-grupperat-först; `build`/`evaluate`/`anyCompatiblePathIncludes` är ordnings-agnostiska och behövde inte ändras. **Konsekvens:** en chansnod vars sannolikhet genuint beror på kontext (villkorstabell som skiljer sig mellan grenar) kan inte längre flippas — dess bestämmande förfader kan hamna **efter** den i den vända ordningen, så vilken fördelning som gäller är odefinierat vid byggtillfället → `FlipError` ("olika sannolikheter beroende på kontext"), fail-loud, inte en gissning. En chansnod med en enda, kontextoberoende fördelning (vanligaste fallet, och det enda länkad-grupp-sannolikhetssynk producerar) påverkas inte. `ensureVocInvariant` (VOC ≥ 0) anropas inte längre internt — full vändning garanterar inte att EV ökar (en flyttad beslutsnod kan hamna sämre ställd), så VOC kan nu vara legitimt negativt (hanteras av absolutbelopps-VOC:en, se ovan). De fem "bevisade" scenarierna (`bayesReversal.scenarios.test.ts`) räknades om: scenario 1 (2 variabler, olika typ) och scenario 4 (samma) är omöjliga att skilja från gamla algoritmen och oförändrade; scenario 2 (villkorstabell C2 beror på C1) failar nu loud istället för att ge VOC=1 — det var just den avsedda konsekvensen; scenario 3 och 6 (samma-fördelning länkade grupper) gav **samma** siffror trots ny ordning (strukturell symmetri gör ordningen irrelevant för slutresultatet i de fallen); scenario 5 (djupt blandat träd) fick nya regressions-pinnade tal, "VOC ≥ 0"-antagandet borttaget.
- ✅ **ALGORITMÄNDRING: segmentvis vändning — villkorstabell-noder som brytpunkter (2026-08-04, segment 29, generaliserar segment 22):** flip/VOC fungerar nu **även med villkorstabeller**. En nod med villkorstabell är en **vägg** (fast pivot): sekvensen på **var sida** om väggen vänds oberoende, väggen står kvar (`a→b→C[villkor]→d→e` blir `b→a→C→e→d`). En väggs bestämmande förfäder ligger på lägre djup → i ett tidigare segment → fortfarande **före** väggen efter vändningen, så dess kontext är alltid upplösbar. Ett träd **utan** villkorstabeller har inga väggar → ett enda segment → hela sekvensen vänds, **exakt som segment 22** (byte-identiskt). Implementation: `CanonicalVar.hasTable` (sant om någon instans på nivån har en villkorstabell), `segmentReverseOrder(canonical)` (vänder varje icke-vägg-löpa, väggar fixa), och `buildFromOrder` löser en väggs kontextberoende fördelning **per-gren** vid byggtillfället (`resolveWallDist` går originalträdet längs den aktuella tilldelningen till rätt instans + historik, `resolveProbability` per utfall). `collectCanonical` kastar inte längre `FlipError` för villkorstabell-noder (de hanteras som väggar) — men en nod **utan** tabell vars bas-sannolikheter ändå skiljer sig mellan instanser (genuint inkonsekvent variabel) failar fortfarande loud. Scenario 2 (`bayesReversal.scenarios.test.ts`) räknades om: flippar nu segmentvis (rot = D, C2 vägg med upplöst 0.9/0.1 resp. 0.1/0.9, VOC=|9−5|=4) istället för att kasta; nytt scenario 7 verifierar vägg-i-mitten med segment på **båda** sidor.
- ✅ **Villkorstabeller kan bero på FLERA förfäder (2026-08-04, segment 29):** modellen stödde redan multi-token-villkor (`matchRow` matchar villkoret som en delmängd av hela vägen, mest specifik vinner) och icke-förälder-förfäder (`availableTokens` ger alla förfäder på vägen). UI:t byggde bara enkel-token-villkor; nu är villkors-pickern **kryssrutor** (en per åtkomlig förfader-token) — kryssa flera → AND (t.ex. "stormigt OCH nederbörd"). `availableTokens` ger bara vägens tokens (en per förfader-variabel), så två kryss är alltid olika förfäder — ingen motsägelsefull samma-variabel-kombination möjlig.
- ✅ **UX-polish (2026-08-01):** split-läget öppnas direkt av Flip (inget extra klick för VOC), Flip-knappen har en tooltip som förklarar vad den gör, och en alltid synlig hint-rad under VOC-raden förklarar i en mening att **VOC = värdet av klarsyn** (hur mycket EV ökar om alla slumputfall är kända innan besluten). "Sammanfoga" återgår utan att förlora vänsterträdets redigeringar (verifierat i test).
- ✅ **Fail-loud även för ofullständiga träd (2026-08-01):** ett strukturellt giltigt men ofullständigt träd (saknade sannolikheter/payoffs) kastar ingen `FlipError` — då blir EV:erna `NaN`. Tidigare visades bara ett tyst "VOC = –"; nu visar VOC-raden **varför** ("fyll i alla sannolikheter och utfallsvärden …"), i både EV- och EU/CE-läge. Alla `FlipError`-meddelanden är översatta till svenska (var tidigare engelska i ett annars svenskt UI).
- ✅ **Länkade grupper viks ihop till EN gemensam nod (verifierat 2026-08-02):** en länkad variabelgrupp identifieras via `variableId` och Bayes-omvänds som *en* delad osäkerhetskälla i det omvända trädet (inte en kopia per instans). Efter sannolikhetssynk-designändringen delar en grupp utan villkorstabeller garanterat *en* fördelning, så hopvikningen är entydig. En grupp där en instans avviker (villkorstabell → beslutsberoende fördelning) failar loud med `FlipError` ("fördelningen skiljer sig mellan grenar") istället för att fabricera en nod — klarsyn är odefinierad när slumpfördelningen beror på beslutsvägen. Väldefinierade villkorstabeller (beroende på en chans-förfader, konsekvent över beslutsgrenar) hanteras korrekt via `chanceContextKey` (scenario 2). Reversal-algoritmen behövde inte skrivas om; root cause för det upplevda flip-felet var osynkade sannolikheter (nu åtgärdat av designändringen).
- ✅ **Matematiken verifierad över fem scenarier (2026-08-01, `bayesReversal.scenarios.test.ts`, bevisbörda):** (1) symmetriskt VOC=2, (2) **villkorstabeller** VOC=1 — konstruerat så att en naiv marginalomvändning i stället skulle ge 5, alltså bevisas att villkorade sannolikheter respekteras genom hela reversal-processen (legacy-buggen finns inte kvar), (3) nästlade länkade grupper VOC=1.2, (4) asymmetriskt/dupliceringsregeln VOC=0.7, (5) djupt 4-nivåers blandat träd (interna invarianter + oberoende omräkning). Ingen matematikbugg hittad.

## Rendering / UI (interaktionsmodell reviderad 2026-07-27 efter legacy-analysen)

- ✅ SVG-rendering av trädet (`src/render/`), fullt idempotent redraw (rensa + bygg om, inga ackumulerande lyssnare). Former, inte färger: rektangel = beslutsnod, ellips = slumpnod, triangel = terminalt utfall (med payoff och joint path-sannolikhet).
- ✅ Bézier-kurvor för grenar, etiketter med upplöst sannolikhet. Etiketter placeras vid t=0.75 längs kurvan där syskonkurvor divergerat — verifierat separerade vid 3–4 utfall per nod (legacy-buggen). Beslutsnoders alternativ visar ingen sannolikhet.
- ✅ Zoom/pan begränsad till canvas-ytan (transform på SVG-viewporten; toppmenyn är vanlig HTML utanför). Dubbelklick återställer zoom.
- ✅ Leaf-spacing auto-expand mot overlap, omräknas varje redraw.
- ✅ **Länk-tillstånds-badge (2026-08-02, segment 20):** en liten glyf i nodens hörn gör länk-tillståndet synligt i trädet — **⛓** för en synkad länkad instans, **⊞** för en tabellstyrd (opt-out); ingen på singletons. `<title>` ger hover-förklaring. Render-koden räknar `variableId`-förekomster själv; ren CSS-klass `.node-badge` (dämpad, ingen ny färgsemantik), temat oförändrat.
- ✅ **Pill-sekvensbar per panel — NIVÅ-omordning (2026-08-04, segment 23; ersätter segment 21:s syskon-utfalls-omordning):** varje panel (vänster + höger) har en egen pill-bar dockad överst i canvas-ytan med **en pill per NIVÅ** (variabelsekvensen — depth 0, 1, 2 …), `→`-pilar för traverseringsriktning, härledd ur varje träds verkliga struktur (`levelSequence`), aldrig hårdkodad per sida. En länkad grupp är **en** nivå-pill (ingen separat `a'`-pill). **Drag-och-släpp** på en **intilliggande** pill byter plats på de **två nivåerna** och omstrukturerar det **faktiska trädet på plats** (`api.reorderLevels` → `reorderAdjacentLevels` i `bayesReversal.ts`, som återanvänder flip-byggets kärna med en swap:ad `order`-array istället för reverserad; länkade grupper bevaras via `preserveGroups`). Drop på icke-granne eller i fel panel ignoreras (grann-endast, användarval). **Villkorstabeller (uppdaterat 2026-08-04, segment 30):** en villkorstabell-nivå är inte längre helt låst — den är en "vägg" (`.wall`, prickad understrykning) som **kan** dras förbi nivåer den **inte** beror på (fall 2), men **inte** förbi en nivå dess tabell beror på (fall 1 → `reorderAdjacentLevels` kastar `flipReorderDependency`). Villkorstabellen **bevaras** genom ombyggnaden: `buildFromOrder(..., preserveTables=true)` kopierar tabellen och **ommappar** dess villkors-tokens (`gammalt-förfader-id:utfall` → nytt id) via en `pathNodes`-karta + `oldIdToVar`. En nivå som inte är en enhetlig variabel (**frikopplad** instans) är fortfarande hårt låst (`.locked`, streckad) — `collectCanonical` kastar. Nya noder får `n<siffra>`-id via appens `nextId`; vald nod nollställs (ny struktur). **Klick på en pill** öppnar samma kontextmeny som nodklick (för nivåns primär-instans) men **utan** Villkorstabell och Koppla loss (`openNodeMenu(..., { fromPill: true })`). Ersätter backlog-punkten "node-omflyttning". *(Segment 21:s syskon-utfalls-omordning `reorderOutcomeInGroup` togs bort — ingen annan ingång fanns, och nivå-omordning är vad användaren ville ha.)*
- ✅ **Papper/rit-tema (2026-08-02, vidareutveckling av det tidigare svart-vitt-kravet):** hela appen — canvas, bars, dialoger, meny — delar ett sammanhängande "drafting"-designspråk, styrt av **design tokens** i ett tydligt avgränsat `:root`-block överst i `src/style.css` (byt tema = byt tokens; inga hårdkodade färger/typsnitt/linjetjocklekar nedströms, och render-koden innehåller inga färger alls — bara CSS-klasser). Helt vit bakgrund `#ffffff` (användarval); mjuk sepia-bläck `#2a2621`; hårfina 1.1px-linjer på noder/kurvor; **serif** (Georgia-stack) för rubriker/etiketter (titel, nodnamn, menyval, dialogrubriker, pane-caption), **monospace** (SF Mono-stack) för alla numeriska readouts (EV/CE, sannolikheter, trace, VOC, γ). Nodformer **oförändrade** (ellips = chans, rektangel = beslut) — bara linjetjocklek/färg justerad. **Siffror är svarta** (bläck) — EV/CE, sannolikheter, joint-p, trace, γ (användarval: ta bort färg från tal). **Funktionell färgsemantik:** dämpad terrakotta `#b0682f` för varningar/ofullständig data (Σ-varning, dialog-varning, flip/utility-fel) är den enda betydelsebärande färgen på tal-nära element; dämpad skiffer-blå `#40597a` signalerar nu bara länk-synk-info (sync-note). Strukturell **accent** dämpad tegel `#8a453a` för emfas (primärknapp, vald nod, sync-regel). Rörelse minimal/mekanisk (korta linjära `100ms`-transitions på knappar, inga studsiga kurvor). Legacy's lila villkorsdialog upprepas inte.
- ✅ **Legacy's interaktionsmodell:** klick på nod → kompakt kontextmeny (Byt namn / Redigera utfall / Växla typ / Villkorstabell / Ta bort nod), varje val öppnar en fokuserad dialog. Klick på terminalt utfall (triangel) → dialog med payoff + mål-joint-sannolikhet (backward-fill) + "Lägg till barnnod" (så växer trädet — äkta asymmetriska träd). Utfallsredigeraren har en **explicit** Normalisera-knapp och Σ-varning — normaliserar aldrig tyst. Villkorstabellen redigeras som matris (rader = villkor valda ur förfädernas utfall, kolumner = nodens utfall, "(bas)"-raden = bassannolikheterna).
- ✅ Live EV per nod och upplöst sannolikhet per utfall — uppdateras direkt vid varje ändring, ingen "beräkna"-knapp. Ofullständig data (osatt sannolikhet/payoff, nod utan utfall) visas som "–", aldrig fabricerade värden. Nya utfall skapas med **osatt** (NaN) sannolikhet, inte 0. Backward-fill-rapporter och fel visas i meddelanderaden under toppmenyn.
- ✅ **Speglad layout för höger träd i split-läge (2026-08-02):** klarsynsträdet ritas horisontellt speglat — roten vid höger canvas-kant, grenar växer åt vänster (mot mitten), lövtrianglar pekar vänster, Bézier-kurvorna böjer åt vänster. Endast den **rumsliga** layouten speglas: `mirrorLayout(layout, axisWidth)` reflekterar varje x till `axisWidth − x` (noder, kant-ändpunkter, etikett-x), medan `renderTree` fortsatt ritar **texten upprätt och läsbar** (labels/EV/sannolikheter, ingen `scale(-1)`-transform — bara ankare/triangelriktning vänds). Reflektionsaxeln är `max(innehållsbredd, canvas-bredd)` så trädet höger-justeras mot canvas-kanten (litet träd kramas inte mot mitten). Same-riktnings-layouten var ett medvetet tidigare val men live-användning visade att det saknade visuell signal om att höger är det omvända trädet; C#/WPF-referensen (`feature/child-based`) använde samma geometri (`ColumnXs[i] = canvasW − ColumnXs[i]`). Idempotent redraw och oberoende zoom/pan bevaras (speglingen sitter i layout-koordinaterna, inte i en vy-transform).

## Spara / ladda

- ✅ Serialisering/deserialisering (`src/model/serialization.ts`) round-trip-testad, snake_case-fältnamn (`conditional_tables`). Osatt sannolikhet (NaN) och osatt payoff mappas **explicit** till `null` och tillbaka.
- ✅ Spara/ladda-UI (`src/model/document.ts` + app.ts): 💾 Spara laddar ner ett dokument (`{ tree, rightTree, rightEdited, split, displayMode, utility, idCounter }`, JSON, Blob-nedladdning, filnamn = rotnamn + datum); 📂 Ladda öppnar filväljare, validerar + applicerar + ritar om. Round-trip exakt inkl. länkade variabelgrupper (`variableId`/`instanceIndex`), villkorstabeller och EV/EU-inställningar. Validering är fail-loud (ogiltig JSON, okänt/saknat format, felaktig display_mode/utility/nodform, trasig variabelgrupp) — laddar aldrig ett partiellt/trasigt träd. Dirty-flagga + bekräftelse innan en laddning skriver över osparade ändringar. **Båda träden sparas (dokumentversion 2, 2026-08-03):** eftersom höger träd nu är fritt redigerbart måste dess redigeringar överleva Spara→Ladda — annars vore det tyst dataförlust. `right_tree`/`right_edited`/`split` round-trip-serialiseras; v1-filer (utan dessa fält) laddas fortfarande som enkel vy. En laddning **regenererar inte** höger (till skillnad från Flip) — den återställer det sparade trädet exakt.
- Path-keyed datastrukturer för delade Outcome-objekt över flera paths: **inte en risk idag** (trädet är strikt ett träd, ingen delning), men bli explicit designkrav om delning införs senare.

## Undo/redo

- ✅ **Snapshot-baserad historik (2026-08-02):** `Ctrl+Z`/`Cmd+Z` = ångra, `Ctrl+Shift+Z`/`Cmd+Shift+Z` (även `Ctrl+Y`) = gör om, plus ↶/↷-knappar i toppmenyn (gråas ut när historiken är tom i den riktningen). Vid varje committad mutation deep-clonas hela dokument-state (**båda** träden + rightEdited + split + displayMode + utility + idCounter, via den testade `documentToJson`-round-trippen) plus vald nod; snapshotten tas i **en central choke point** (slutet av `render()`, gated av en `pendingCommit`-flagga som `markDirty`/`toggleSplit` sätter). Därför blir hela effekten av en åtgärd **ETT** steg även när den skapar många noder: auto-fyll/rekursiv mirroring, sannolikhetssynk över en länkad grupp, nodtyp-synk, "Koppla loss" — allt kollapsar till ett steg eftersom det sker före det enda `render()`-anropet. Flip/toggle split är också ett steg (ångra återställer split-läget, inte bara vänstra trädet). **Koalescering:** sannolikhets-/payoff-redigering committar bara vid dialog-spara (inte per tangenttryckning), så kontinuerlig redigering av ett fält är redan ett steg. **Ladda (📂)** nollställer historiken (ny baseline — kan inte ångra tillbaka in i förra dokumentets state); **Spara (💾)** rör inte historiken (bara export). Ny mutation efter ångra rensar redo-stacken. Tangentgenvägarna hoppar över app-undo helt när fokus ligger i ett `input`/`textarea`/`select`/contenteditable (då gäller fältets egen textundo). Historiktak 100 steg — äldsta faller bort tyst.

## Export

- ✅ **PNG-export med transparent bakgrund (2026-08-04, segment 25):** trädet kan sparas som en PNG-bild med **transparent bakgrund** — bara text/grafik synlig. **UI (segment 26):** en enda **💾 Spara ▾-dropdown** (ersätter de tidigare två separata knapparna) med två val — **"Spara som fil"** (JSON) och **"Spara som PNG"** — byggd med samma `.menu`/`.menu-item`-komponent som nodmenyn (ankrad under knappen, click-away stänger), i linje med appens estetik. **I split-läge exporterar PNG-valet BÅDA träden som var sin egen PNG** (`<namn>.png` för vänster + `<namn>-omvant.png` för det omvända högerträdet, med sin verkliga speglade layout); utanför split bara huvudträdet. `src/render/pngExport.ts`: klonar canvas-SVG:n, återställer viewport-transformen (exporterar hela trädet i **naturlig** storlek, oberoende av nuvarande zoom/pan), fittar `viewBox` till innehållets `getBBox` + marginal, och **inline:ar beräknade stilar** (`getComputedStyle` per element → `fill`/`stroke`/`stroke-width`/typsnitt/…) eftersom extern CSS inte följer med en fristående serialiserad SVG — så bilden matchar temat på skärmen. Rastreras via `Image`→`<canvas>` i **2×** (skarp) utan `fillRect` (canvas är transparent bakom trädet) → `toBlob('image/png')` → nedladdning (filnamn = rotnamn + datum, `.png`). Systemtypsnitten (Georgia/SF Mono-stackar) finns lokalt → ingen extern font att bädda in. Filnamn härleds via `documentFilename` (samma säkra namn som JSON, `.png`). **Live-verifierat:** exporterad PNG har alla fyra hörn alpha 0 (transparent), ~20k bläck-pixlar (trädet ritat med korrekt styling), 684×250 px för ett litet träd. Tomt träd → "Inget träd att exportera". *(Testtäckning: knappens närvaro + tomt-träd-meddelande i jsdom; själva rastreringen — `getBBox`/`canvas`/`Image` finns inte i jsdom — är live-verifierad.)*

## Expected Utility (EU)

- ✅ EU-beräkning (väntad nytta) som alternativ till EV (`src/model/expectedUtility.ts`, `src/model/utility.ts`).
- **U-funktion (reviderat 2026-07-27 efter kursmaterialet):** endast **två** former — linjär (riskneutral, u(x)=x, CE=EV) och exponentiell CARA u(x)=(1−e^(−γx))/γ. Parametern heter **γ**; kursens "riskodds" r relaterar via γ = ln(r) (γ>0 riskavert, γ=0 neutral, γ<0 risksökande). Kvadratisk/logaritmisk från den tidigare fyra-typs-versionen är borttagna (utanför kursmaterialet). Båda har sluten invers för CE.
- Terminala outcomes payoff-värden transformeras genom u-funktionen → EU räknas som EV men på nyttovärdena (samma decision=max/chance=weighted-average-rekursion) → CE = u⁻¹(EU) i pengar för visning. Osatt payoff → NaN → "–". Ogiltig invers (utility utanför räckvidd) kastar `UtilityDomainError` och visas som banner + "CE –", ingen krasch.
- **γ-elicitering (kursens metoder, `src/model/utility.ts`):** (1) indifferens-fråga — användaren anger p där hen är indifferent mellan 0 säkert och (p: vinn 1 / 1−p: förlora 1) → γ = ln(p/(1−p)) exakt; p=0.5 → γ=0. (2) snabb approximation — referensbelopp W → γ ≈ 0.96/W. UI:t visar resulterande γ, riskodds r, och exempel-CE:n; γ kan finjusteras manuellt efteråt.
- UI: lägesknapp EV ↔ EU/CE i toppmenyn; i EU-läge visar noder CE (pengar, aldrig råa nyttotal). Utility-bar med typval + γ-input + elicitering-dialog. Gäller även split-läge: VOC beräknas då på CE som `|CE_vänster − CE_höger|` (2026-08-03, riktningsagnostisk — se Flip/split).

## Explicit ur scope tills vidare

- Ingen backend/server — allt körs client-side, statisk deploy.
- Ingen C#/.NET-kod behålls.
- Ingen Python-kod behålls (kan användas som referens för logik, men porteras inte rakt av).
